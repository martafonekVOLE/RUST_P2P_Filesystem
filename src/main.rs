mod cli;
mod config;
mod core;
mod networking;
mod routing;
mod sharding;
mod storage;
mod utils;

fn main() {
    // Init node struct

    // Run node.start_listening()

    // Parse input from the CLI - ping node_id, find_node node_id in a loop
}
